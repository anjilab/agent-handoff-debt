# Generated data

This directory is created by local experiment commands. It may contain Docker workspaces, model trajectories, validation outputs, and summaries. Generated data is intentionally excluded from version control.

The public repository includes the source code, agent profiles, task configurations, prompt template, and selected task list needed to generate these artifacts in a compatible environment.
