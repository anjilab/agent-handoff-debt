# Third-party notices

The selected task prompts and task metadata in `tasks/swebench_verified/` and
`configs/tasks/swebench_verified/` are derived from
[SWE-bench Verified](https://huggingface.co/datasets/princeton-nlp/SWE-bench_Verified).
They are included only to reproduce the selected 75-task study setup. Users are
responsible for following the terms of SWE-bench Verified and the licenses of
the upstream repositories represented by its task instances.

This repository uses OpenHands-compatible interfaces and configuration. Users
must separately install and comply with the licenses of OpenHands, Docker, and
any model or evaluation images used for experiments.
